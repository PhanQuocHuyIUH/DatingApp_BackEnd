### Install dependencies

```
npm install
```

### To start server

```
npm run dev
```

## 🎯 ROADMAP CHO TỪNG SCREEN

| Screen                       | Backend API cần                                            |
| ---------------------------- | ---------------------------------------------------------- |
| **Screen 3: Login**          | `POST /api/auth/login`, `/api/auth/register`               |
| **Screen 6: Swipe**          | `GET /api/discovery/profiles`, `POST /api/discovery/swipe` |
| **Screen 8: Profile Detail** | `GET /api/users/:id`                                       |
| **Screen 10: Filters**       | `PUT /api/users/preferences`                               |
| **Screen 13: Matches**       | `GET /api/matches`                                         |
| **Screen 12: Chat**          | `GET /api/chats/:id/messages`, Socket.IO                   |
| **Screen 4: Profile**        | `GET /api/users/me`                                        |
| **Screen 9: Edit Profile**   | `PUT /api/users/me`, `POST /api/upload`                    |

---
